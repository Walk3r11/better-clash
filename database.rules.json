{
  "rules": {
    ".read": "auth != null", 
    ".write": "auth != null", 
    "decks": {
      "$userId": {
        ".read": "$userId === auth.uid",
        ".write": "$userId === auth.uid" 
      }
    }
  }
}